# Intrusion tileset — slot table
#
# One line per allocated slot of `tiles.png`: index, key, description.
# A slot's index is `row * 16 + col`, and **an index is permanent** — art is
# drawn against a slot number, so moving one silently repaints every cell that
# referenced it. Claim the next free slot; never close a gap.
#
# Bands, with room left after each. Free slots start at 32.
#
#     0-15   one sprite per glyph (§11.3)
#    16-31   the wall autotile run, keyed by a neighbour bitmask
#
# A `glyph:` or `wall:` slot listed here with nothing drawn in it is a slot
# waiting for art; the renderer falls back to drawing the character, which is
# never an error.
#
# A `rotation:` slot is different: it is allocated and **deliberately empty**.
# Its neighbourhood is another slot's shape turned a quarter or two, and the
# shell turns it at draw time, so drawing it again would be storing the same
# image twice. The slot stays claimed because the band is indexed `16 + mask`
# and closing the gap would slide every slot after it.
#
# Seeded by `scripts/seed-tileset.py`; hand-edited from here on.

0     glyph:#      Wall
1     glyph:□      Building fabric on the plans, never seen (§11.5a)
2     glyph:+      Door panel, closed
3     glyph:×      Door frame (hinge)
4     glyph:}      Cupboard — a hiding place
5     glyph:π      Table — partial cover
6     glyph:=      Duct mouth
7     glyph:·      Floor, inside your field of view
8     glyph:E      The exit — your own tunnel's mouth
9     glyph:$      Intel console
10    glyph:Ψ      Comms console
11    glyph:@      You, and a decoy you placed
12    glyph:g      A guard you can see
13    glyph:z      A body
14    glyph:¤      Equipment cache — salvaged tech (§2.2/§14 v3)
16    wall:0       Wall autotile — wall neighbours none
17    wall:1       Wall autotile — wall neighbours N
18    rotation:2   Wall autotile — wall neighbours E: slot 17 turned 1 quarter clockwise
19    wall:3       Wall autotile — wall neighbours N-E
20    rotation:4   Wall autotile — wall neighbours S: slot 17 turned 2 quarters clockwise
21    wall:5       Wall autotile — wall neighbours N-S
22    rotation:6   Wall autotile — wall neighbours E-S: slot 19 turned 1 quarter clockwise
23    wall:7       Wall autotile — wall neighbours N-E-S
24    rotation:8   Wall autotile — wall neighbours W: slot 17 turned 3 quarters clockwise
25    rotation:9   Wall autotile — wall neighbours N-W: slot 19 turned 3 quarters clockwise
26    rotation:10  Wall autotile — wall neighbours E-W: slot 21 turned 1 quarter clockwise
27    rotation:11  Wall autotile — wall neighbours N-E-W: slot 23 turned 3 quarters clockwise
28    rotation:12  Wall autotile — wall neighbours S-W: slot 19 turned 2 quarters clockwise
29    rotation:13  Wall autotile — wall neighbours N-S-W: slot 23 turned 2 quarters clockwise
30    rotation:14  Wall autotile — wall neighbours E-S-W: slot 23 turned 1 quarter clockwise
31    wall:15      Wall autotile — wall neighbours N-E-S-W
